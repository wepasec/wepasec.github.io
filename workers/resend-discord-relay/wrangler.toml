name = "resend-discord-relay"
main = "worker.js"
compatibility_date = "2024-09-01"

# Secrets (set via `wrangler secret put <NAME>`, not stored here):
#   DISCORD_WEBHOOK_URL   - required
#   RESEND_WEBHOOK_SECRET - optional, enables signature verification